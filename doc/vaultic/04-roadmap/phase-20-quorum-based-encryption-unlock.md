# Phase 20: Quorum-based encryption unlock (threshold key custody with offline, hardware, and cloud principals)

[← Back to roadmap index](00-overview.md)

[← Phase 19](phase-19-multi-provider-cold-storage-pool-and-replicated-metadata-store.md) · [Phase 21 →](phase-21-crawl-optimization-with-cwalk-and-pathdiff.md)

**Goal:** replace Phase 18 key-in-DB and independently protected master-key escrow with one pre-database, immutable envelope/recovery capsule that contains authenticated wrapped copies of both the SlateDB metadata DEK and the repository data master key. Protect that capsule with explicit, configurable unlock policies ranging from a single offline bootstrap key to cryptographically enforced $K$-of-$N$ groups and composed expressions containing any supported combination of offline keys, YubiKey-backed local hardware seats, and cloud KMS/HSM keys authorized to individual or group-managed principals: Azure Key Vault / Managed HSM with Entra users or groups, AWS KMS or CloudHSM-backed keys with IAM / IAM Identity Center principals, and Google Cloud KMS or Cloud HSM keys with Google Cloud IAM principals or groups. A small, hardened, long-lived key broker owns the unlock ceremony and supplies separately authorized, scoped key leases to `vaulticdb` and transient `vaultic` jobs, so application restarts and controlled software updates do not require another quorum while loss or restart of the broker or host does. A successful configured unlock must recover the data repository even when SlateDB is missing or cannot initialize, while complete operator documentation covers setup, migration, custody, policy changes, application updates, broker lifecycle, and disaster recovery.

**Current implementation state (2026-09-03): in progress; offline, cloud, YubiKey PIV, and FIDO2 hmac-secret quorum adapters and enrollment, broker/client lease integration, online mixed-policy mutation, broker-identity-loss recovery, guarded key-in-DB migration retirement, and authenticated metadata-rebuild initialization are implemented, but the complete Phase 20 exit criterion is not yet met.** Hardware and cloud paths still require credential-gated release-target validation before the production exit criterion can pass.

Implemented and validated:

* A format-v2 recovery capsule is available before SlateDB and independently wraps the 32-byte metadata DEK and repository master key. Its authenticated logical header binds repository identity, immutable generation, algorithms, key versions, policy hash, canonical logical capsule ID, and the pinned Ed25519 broker identity. HKDF-SHA256 derives purpose-separated wrapping keys and AES-256-GCM authenticates both payloads; unlock releases neither key unless both payloads authenticate.
* Capsule policy evaluation supports `member`, `any_of`, `all_of`, and threshold expressions. The implementation uses the reviewed `sharks` GF(256) Shamir library, fresh per-generation root secrets, recursively distributed policy fragments, zeroizing reconstruction buffers, and complete repository/generation/policy/group/member/index/threshold/share-count/provider bindings. Offline `offline-argon2id` and `offline-keyfile` member creation and contribution are operational. Tests cover every 2-of-3 subset, insufficient shares, composed policies, swapped payloads, foreign repositories, corrupt/re-indexed shares, and location-independent authentication.
* Immutable capsule generations are published with owner-only permissions at deterministic generation-named local paths, discovered by validating all candidate names and authenticated contents, and mirrored with create-only semantics to `_vaultic/recovery-capsules/<generation>.json`. Existing bytes for a generation must match exactly; a conflicting generation fails closed.
* Effective-policy calculation reports the minimum custodian count and the `principal-verified`, `hardware-verified`, and `custody-assumed` assurance classes represented by a capsule. It rejects provider/authority mismatches and flags bootstrap intent, effective 1-of-1 quorum paths, duplicate cloud key references, overlapping immutable cloud principal IDs, duplicate hardware credential IDs, and duplicate hardware public keys. This calculation is exposed by broker status, but repository-wide bypass enumeration in `index keys status` and `index check` remains incomplete.
* The separate `vaultic-key-broker` binary reads the current capsule without opening SlateDB, starts locked, and exposes only a bounded local Unix-socket JSON protocol. It implements signed one-time X25519/HKDF-SHA256/ChaCha20-Poly1305 HPKE contribution sessions, transcript and endpoint binding, expiry, replay closure, duplicate member/share/principal rejection, custodian last-seen-generation rollback rejection, both-payload authentication, unlock epochs, an optional maximum unlocked lifetime, and an explicit lock operation. Active sessions are bounded and expired sessions release all ephemeral state.
* The broker implements distinct `metadata-dek`, `repository-master-key`, and `metadata-loss-recovery` capabilities. Leases are epoch-bound, connection-bound, scoped, expiring, and revoked on disconnect or lock. The broker disables core dumps, locks identity and recovered-key memory where supported, marks recovered-key pages non-dumpable on Linux, zeroizes secret state, broadcasts lock to active connections, and never persists plaintext keys or warm-restart material.
* Broker client authorization uses OS peer credentials and the server-observed executable path and SHA-256 digest, requires a root-owned installation path with no group/world-writable ancestors, verifies an Ed25519-signed release manifest, enforces component identity, release identity, allowed version range, peer UID, capability, and anti-downgrade policy, and rejects caller-supplied identity as sufficient authorization. `vaultic-key-broker identity-init` and `release-sign` create protected broker identity material and signed client release manifests. The normal release build packages `vaultic-key-broker` beside `vaulticdb`.
* `vaulticdb` can obtain a connection-bound metadata-DEK lease before opening encrypted SlateDB and shuts down when that lease expires or the broker disconnects. In broker mode, database master-key get/store routes fail closed. `vaultic` can obtain short-lived repository-master-key or metadata-loss-recovery leases, rejects simultaneous password, direct-key, Azure-secret, insecure no-password, and key-in-DB routes, clears the leased key after repository authentication, and owns the broker connection for the repository lifetime.
* `vaultic index unlock status|contribute|lock` is implemented for signed sessions and offline passphrase/keyfile members. Contribution verifies the capsule-pinned broker signature, endpoint, repository, generation, expiry, and fingerprint before local member unwrap and HPKE submission. The command requires an explicit out-of-band fingerprint value and supports a protected, monotonically increasing custodian generation-anchor file. Accepted/rejected contribution and explicit-lock events are emitted through the existing observability API.
* Azure Key Vault/Managed HSM, AWS KMS/CloudHSM-backed KMS keys, and Google Cloud KMS/Cloud HSM custodian contribution adapters are implemented. External member plaintext is independently bound to the complete capsule/member context before provider wrapping. Contributions validate immutable principal identity and tenant/account/project boundaries. AWS assumed-role sessions normalize to stable IAM role ARNs, AWS CloudHSM members require custom-key-store provenance, and Google Cloud HSM members require an HSM protection-level attestation. Mocked success, identity mismatch, malformed reference, provider denial, context binding, and HSM provenance tests are present; live-provider integration tests remain outstanding.
* Online policy mutation is broker-owned and key-preserving. Signed `policy-mutation` operations accept offline and Azure/AWS/GCP member protections, regenerate the root secret and every share, retain the exact candidate and digest until publication, publish the repository mirror before the local immutable generation, activate only the retained digest, and relock. The five requested threshold convenience commands are implemented, and `--policy-file` supports complete composed `member`, `any_of`, `all_of`, and threshold policies. Interrupted publication is resumable from the exact broker-retained candidate with `resume-mutation`; cancellation is explicit so a partially published immutable generation is not silently orphaned.
* Cloud enrollment is exposed through repeatable mode-0600 JSON definitions that bind provider, versioned key reference, authority, tenant/account/project, and immutable principal ID. Azure and Google bearer tokens are read from separate mode-0600 files and never accepted on the command line; AWS uses the SDK credential chain. Azure JWT audience and expiry and Google token-introspection lifetime are checked after provider acceptance. Mocked mixed offline/cloud mutation proves preservation of both underlying keys and complete context binding.
* Broker identity loss has an explicit recovery mode. It creates a fresh replacement identity, marks every recovery session, requires per-custodian `--unverified-session` acknowledgement and independently compared fingerprints, emits critical authentication events, grants no leases under the stale pin, and requires the next published generation to pin the replacement identity before normal operation resumes. A no-database publisher makes that repin possible when the broker host and SlateDB state are both unavailable.
* Every broker connection now negotiates `vaultic-key-broker.v1` before other operations. Lease acquisition consumes a one-time random challenge whose response binds the negotiated protocol and server-observed executable digest; skipped/repeated negotiation, wrong protocol, wrong executable, and challenge replay fail closed. Both Go and Rust clients implement the exchange.
* `vaultic index keys quorum verify` compares repository ID, generation, logical capsule ID, and policy hash against the running broker's fully validated capsule, then rejects every non-compliant effective-policy finding. `index keys status --capsule` and `index check --quorum-capsule` expose the same effective threshold and assurance classes and mark configured password, direct-key, Azure-secret, no-password, key-in-DB, metadata-passphrase, and standalone metadata-slot routes non-compliant.
* Phase 18 key-in-DB migration has a two-phase RPC and CLI ceremony. Prepare requires clean encrypted metadata, reads the active metadata DEK and retained repository master key, creates and reconstructs the new offline capsule, authenticates both payloads, publishes local and repository mirrors, and records the exact capsule SHA-256 while retaining the old database key. Finalize opens the repository through a newly unlocked `metadata-loss-recovery` lease, authenticates the repository and at least one pack header when packs exist, then proves possession of the capsule-recovered repository key to `vaulticdb` with a domain-separated HMAC over repository ID and prepared capsule digest. Only the exact pending digest and valid proof atomically remove the master-key and migration records; wrong and repeated finalization fail closed.
* Read-only metadata-loss recovery can use a broker-issued recovery lease without writing a plaintext repository key file. Rebuild initialization now requires brokered `required` encryption, a new local candidate directory with no database objects, explicit metadata-loss acknowledgement, and the same broker for repository and metadata-DEK leases. VaulticDB writes the encryption policy and durable recovery provenance before ordinary writes; import performs a full legacy-versus-candidate check before authority activation and emits critical start, validation, and activation events. Service-level destruction/list/restore testing and remote-object-store activation remain outstanding.
* Migration finalization now additionally requires explicit legacy-route retirement and external-escrow-destruction acknowledgements. After capsule and pack proof, it removes all repository password-key records and mirrored standalone escrow records while preserving capsule/envelope metadata, then atomically removes the database key and emits a critical lifecycle event. A retirement failure leaves the database key in place so the operation remains retryable.
* `doc/070_encryption.rst` documents the implemented capsule and broker model, trust boundary, provisioning, unlock ceremony, generation anchors, leases, migration, current recovery behavior, and known limitations. It deliberately retains the Phase 18 material as migration-baseline documentation and does not describe schema-only cloud/hardware providers as available.
* The current implementation passes focused Rust broker/library/daemon and Go broker/CLI tests. Full-suite counts and release/documentation builds are revalidated at the end of each implementation tranche. Rustfmt and Clippy availability remains environment-dependent and requires a clean CI run when the local proxies cannot resolve a toolchain.

Still required before Phase 20 can be marked complete:

1. Complete live cloud-provider integration validation. Custodian-side adapters, generalized operator enrollment, mixed-provider mutation tests, explicit Azure/Google token-expiry checks, and fail-closed provider mocks are implemented. Remaining work is credential-gated testing against real Azure Key Vault/Managed HSM, AWS KMS/custom key stores, and Google Cloud KMS/HSM, plus documented verification of Azure token-claim trust and supported AWS IAM Identity Center/group mappings.
2. Complete credential-gated hardware validation. YubiKey PIV enrollment and contribution are implemented through YKCS11 with token-backed RSA-OAEP, protected PIN input, token-enforced touch, capsule/context binding, and pinned RSA public-key fingerprints. FIDO2 enrollment and contribution use CTAP2 `hmac-secret`, fresh PIN/touch assertions, UP/UV and signature verification, RP/credential/public-key binding, optional verified certificate attestation, and transient zeroized secret outputs. Both support backup tokens as `any_of` alternatives and reject duplicate credentials/public keys. Remaining work requires physical-device testing for wrong token, missing touch, wrong/locked PIN, duplicate seats, backup-token recovery, and supported macOS/Linux release targets.
3. Exercise repository-wide effective-policy validation in production. `index keys status` and `index check` inventory discoverable configured routes and now require a protected, expiring Ed25519-signed operator attestation for persistent plaintext exports, externally copied standalone escrow, stale generation anchors, broker credentials, warm-restart material, and distinct offline custodians. The attestation is bound to repository ID, capsule generation, logical ID, and policy hash, and fails closed when absent, incomplete, invalid, overlong, or expired. Remaining work is an organization-run inventory and evidence review; an attestation cannot independently prove that an undiscovered external copy does not exist.
4. Complete release-target process authorization validation. The strict authorization matrix covers approved signing-key overlap and upgrade, software downgrade, executable replacement, mutable path, non-root ownership, wrong component, wrong UID, and wrong capability. Real Unix peer inspection now verifies kernel-derived UID, running-executable digest, ownership, and path trust on macOS, alongside mutable-path and root-owned system-path checks. Remaining work is to execute the same tests on Linux and exercise the matrix against packaged, installed processes on every release target.
5. Complete authenticated metadata rebuild validation beyond the implemented empty-candidate initialization, durable provenance, consistency gate, local or dedicated-S3-prefix activation, and no-database identity-repin publication. Add credential-gated proof of remote destruction, listing, restore, rebuild, and activation without any plaintext master-key file.
6. Finish Phase 18 retirement evidence. The workflow inventories and removes repository password keys and mirrored standalone escrow only after capsule and pack proof, requires external-copy destruction attestation, retains the database key on retirement failure, rejects conflicting preparation, and exposes durable pending/finalized digests in status while treating pending state as non-compliant. Remaining work is an end-to-end Phase 18 repository proof ending with no master-key record or managed bypass.
7. Complete the `auth`, `integrity`, and `lifecycle` event taxonomy. Broker-side allowlisted JSON events now cover capsule discovery, session creation/expiry, accepted contributions/quorum, request denial, lease grant/release/expiry/disconnect revocation, policy mutation, explicit lock, shutdown lock, and maximum-lifetime automatic lock; clients add contribution member, publication, downgrade, migration, and recovery context. Remaining coverage includes explicit non-expiry session closure, rollback and payload-authentication failures, break-glass, and plaintext export. Broader tests must prove diagnostics contain identifiers and release metadata but never keys, shares, credentials, PINs, tokens, or secret-bearing requests.
8. Add remaining service-level, cross-language, and platform hardening tests. Existing coverage includes a shared Go/Rust external-context fixture, real Unix service negotiation and lock broadcast, and signed mutation prepare/retrieve/reject/activate behavior. Required coverage still includes Go-to-Rust HPKE/Argon2 fixtures, broker/client restart within and across epochs, crash and host-reboot simulation, lease reacquisition, maximum epoch lifetime, memory/core-dump behavior where testable, inherited descriptor/environment/command-line secret absence, Linux sandbox/service configuration, and supported macOS/Linux release targets.
9. Complete operator documentation for cloud IAM/RBAC, YubiKey PIV/FIDO2 enrollment and backup tokens, one-seat-per-group and non-overlap custody rules, signed upgrades and signing-key rotation, break-glass operation, total metadata destruction and remote rebuild, bootstrap to offline 2-of-3 to cloud 2-of-4 migration, key rotation/rewrite, and periodic recovery exercises. Hardened systemd and launchd broker templates and installation guidance are present; Linux unit verification, organization-specific macOS sandbox policy, and production runbook exercises remain.

Until those items are complete, the implemented system supports tested offline bootstrap and offline quorum workflows, YubiKey PIV and FIDO2 software integration, and exercising the brokered migration path, but it does not satisfy the production exit criterion. In particular, software tests and status labels are not substitutes for credential-gated cryptographic operations and identity proof on every supported release target.

**Design:**

A versioned envelope/recovery capsule is stored outside SlateDB at a deterministic bootstrap location beside the database object store, mirrored into the repository backend, and exportable as ciphertext for offline custody. It is readable before SlateDB starts and is the sole managed authoritative copy of two secrets: the metadata DEK used by `EncryptedObjectStore` and the repository master key used to authenticate and decrypt data packs. The repository master key is no longer stored in a SlateDB `meta:` record. Quorum-compliant mode removes ordinary repository password keyfiles, standalone single-provider master-key escrow, and unattended `--key`, `--key-file`, or `--key-command` paths; explicit emergency export remains a non-compliant, high-severity audited action because any plaintext master-key copy bypasses the configured quorum.

The quorum ceremony unlocks a dedicated `vaultic-key-broker`, not `vaulticdb` or a crawl process. The broker is the only component that reads and unwraps the capsule, reconstructs its root secret, and holds both recovered keys between client lifetimes. It starts locked, remains resident across authorized `vaulticdb` and `vaultic` restarts or upgrades, and defines one unlock epoch ending on explicit lock, broker exit or crash, host shutdown or reboot, capsule-policy activation that requires relock, or an optional configured maximum unlocked lifetime. It never persists unwrapped keys or warm-restart material. Consequently, a quorum-compliant deployment has no unattended **broker** startup path, but restarting an authorized application client while the same broker unlock epoch remains active does not require a new ceremony.

Clients use local-only authenticated IPC and receive least-privilege leases rather than capsule access. `vaulticdb` may obtain only the metadata DEK, bound to its authenticated process connection and reacquirable after its restart; a `vaultic` crawl or recovery job may obtain only the repository master key, or a narrower derived capability where the repository format permits it, under a short-lived job lease. The initial implementation releases key bytes into the authorized client's protected memory for compatibility and throughput; making the broker perform bulk pack or object cryptography is an optional stronger mode, not required for Phase 20, because it would substantially expand the broker API and hot-path IPC. Lease expiry limits duration and scope but cannot stop a compromised authorized client from copying a key while leased.

The broker is the central post-unlock trust boundary and must remain substantially smaller than either client. Its IPC authenticates OS peer credentials and an approved component identity, checks executable ownership and a non-writable installation path, and verifies a signed release or deployment identity with component name, allowed-version, and anti-downgrade policy so controlled client updates do not silently broaden authorization. Authorization is never based only on a mutable path, Unix account, caller-supplied name, or executable hash that cannot support controlled replacement. The service runs as a dedicated least-privilege account with local socket ACLs and platform sandboxing, locks secret memory where supported, excludes it from core dumps and diagnostic capture, avoids secret-bearing environment variables, command lines, inherited descriptors, and logs, and zeroizes keys, shares, and leases on release or shutdown. Root, kernel, debugger, broker, or authorized-client compromise during an unlock epoch can extract or use the corresponding keys; quorum protects cold state and creation of a new unlock epoch, not a fully compromised running host.

Each capsule is encrypted under a random per-generation root wrapping secret. Independent wrapping keys are derived with HKDF using the repository UUID, capsule format and generation, root-key version, and distinct purposes (`metadata-dek` and `repository-master-key`), then used with authenticated encryption to wrap the two secrets separately. This domain separation prevents one wrapped payload or implementation path from being substituted for the other. The complete capsule header and both ciphertexts are authenticated and bind repository UUID, envelope generation, algorithms, key versions, unlock-policy hash, and the canonical logical capsule identifier — deliberately not the physical storage location, so the local copy, repository mirror, and offline export of one generation authenticate identically. The authenticated policy also pins the public half of a long-term broker identity keypair used to sign unlock sessions. Unlock succeeds only if both payloads authenticate; partial recovery must never grant either client lease.

An unlock policy is an explicit expression of alternatives and requirements, using `any_of`, `all_of`, and threshold groups. An alternative may be a single offline bootstrap member, a threshold group, or a composed policy such as `(1-of-2 YubiKeys) AND (one separate offline password)`. A threshold group protects the capsule root secret with Shamir secret sharing from a reviewed implementation over a suitable finite field: the root secret is split into $N$ shares with reconstruction threshold $K$, and each share is wrapped independently under one member's protection. Offline members protect a share with an Argon2id-derived KEK from a per-custodian passphrase or with a keyfile on removable media. Hardware members protect a share with a non-exportable key or hardware-derived secret on one or more YubiKeys; the intended personal pattern is a hardware seat where either of two enrolled YubiKeys may unwrap the same member share, combined with a separate password/offline seat so neither the password nor one YubiKey is sufficient alone. Cloud members protect a share with a dedicated versioned cloud KMS/HSM key: Azure Key Vault / Managed HSM through `wrapKey`/`unwrapKey`, AWS KMS through `Decrypt` with an encryption context or AWS CloudHSM-backed key material exposed through an approved wrap/unwrap adapter, and Google Cloud KMS / Cloud HSM through `decrypt` with purpose-bound AAD. Provider IAM scopes each key to one quorum principal or duty group (Entra, AWS IAM / IAM Identity Center, or Google Cloud IAM), while the broker's standing identity can unwrap no cloud share. Each share binds the repository, capsule generation, policy and group IDs, share index, threshold, share count, root-key version, member ID, provider, and key reference, so shares from different repositories, generations, policies, or resharings cannot be combined.

Policies support one or many alternatives such as normal `2-of-4` cloud custodians, break-glass `2-of-3` offline custodians, and personal or small-office policies like `all_of(any_of(yubikey-primary, yubikey-backup), offline-password)`. Every membership, threshold, member-key rotation, or policy change requires an already unlocked broker, generates a fresh root wrapping secret and fresh shares, rewraps both capsule payloads, and publishes and mirrors a new immutable generation without changing either underlying secret. Status reports the minimum effective threshold across every current access path; a retained 1-of-1 bootstrap member, password keyfile, direct master-key copy, single-key escrow, overlapping cloud-principal authorization, duplicate hardware-seat enrollment, or other complete-key bypass makes the repository non-compliant and must never be presented as four-eyes protected. Seat separation is verifiable only for cloud members whose contributions carry validated immutable principal identifiers from Entra, AWS IAM / IAM Identity Center, or Google Cloud IAM. YubiKey-backed seats are hardware-presence verified by pinned public key, serial number where available, attestation where configured, PIN policy, and touch/user-presence proof, but they are not a distinct-human guarantee unless paired with an independent authenticated principal. Offline seats have no verifiable holder identity, so one person holding two offline shares defeats an offline quorum undetectably. Effective-threshold output therefore labels cloud groups as principal-verified, YubiKey seats as hardware-verified, and offline groups as custody-assumed, with human separation enforced by the documented custody runbook. A quorum-compliant repository consequently has no unattended broker startup path: every broker or host restart blocks in the locked state until a quorum completes, persisted warm-restart unlock material is a bypass and is prohibited, and the documentation must state these on-call expectations explicitly. Authorized client restarts and updates reuse only newly authenticated leases from the live broker and never receive persisted broker state.

Because a locked broker cannot call custodian-scoped cloud keys itself or operate a local YubiKey without the operator's PIN/touch ceremony, unlock uses an interactive contribution session. Capsule ciphertext is deliberately non-secret and widely replicated, so the primary residual attack is a fake locked broker phishing custodians for shares; sessions are therefore authenticated, not merely encrypted. The broker reads the capsule without opening SlateDB and creates a one-time session using an authenticated, reviewed HPKE construction with a fresh ephemeral key, nonce, transcript, and expiry, signed by the broker identity key pinned in the capsule policy. `vaultic index unlock contribute` verifies that signature against the capsule, displays a short session fingerprint for out-of-band confirmation between custodians, and embeds in the signed transcript the capsule generation the custodian's tooling last observed. Each custodian authenticates directly with the relevant cloud identity plane (Entra, AWS IAM / IAM Identity Center, or Google Cloud IAM), performs the YubiKey PIN/touch operation, or opens an offline share locally, unwraps only their member share, encrypts it to the verified session, submits it, and zeroizes local plaintext. YubiKey support may use PIV private-key unwrap/key agreement or FIDO2 `hmac-secret`/PRF, but the resulting construction must keep the private key or PRF seed non-exportable, pin the enrolled credential/public key, bind the relying-party or certificate identity to the repository and member ID, and never treat a certificate signature as sufficient unless it gates the encrypted share. The broker validates cloud tenant/account/project boundaries and immutable principal identifiers where applicable and rejects unsigned or wrongly signed sessions, duplicate share indexes, duplicate contributing principals, duplicate hardware-seat contributions, foreign transcripts, expired or replayed contributions, and contributions for another repository or generation. After the policy expression is satisfied it reconstructs the root secret in locked memory, authenticates and unwraps both the metadata DEK and repository master key, zeroizes all intermediate material, permanently closes the session, and begins a new unlock epoch. Because the identity private key necessarily rests in plaintext on the broker host, this raises share harvesting from "anyone holding public capsule ciphertext" to "an attacker controlling the broker host who still deceives the configured quorum"; the fingerprint ceremony and custodian generation checks address that remainder.

The recovered repository master key is available independently of SlateDB health. If metadata opens, normal operation continues with both keys held only in protected broker memory except while copied into an authorized active lease. If SlateDB is absent, corrupt, or fails authentication, an explicitly authorized Vaultic recovery job can lease the already recovered repository master key to enter read-only metadata-loss recovery, inspect or restore packs through the legacy index, and rebuild metadata without writing a plaintext key file. Loss of the broker host also loses the identity private key, so disaster recovery must not deadlock on session verification: contributing to an unsigned session requires an explicit per-custodian `--unverified-session` acknowledgement, mandatory fingerprint confirmation, and a high-severity `auth` event, and the first post-recovery capsule generation pins a fresh identity key. Use of an alternative marked recovery/break-glass requires explicit acknowledgement and emits a high-severity `auth` event. Capsule ciphertext is not secret without a quorum and is replicated aggressively, but loss of every capsule copy is unrecoverable even if all custodians retain their shares.

Ordinary policy changes are online custody operations and do not re-encrypt SlateDB objects or data packs. They do not, however, revoke an old capsule generation already copied together with enough old member credentials. Rollback protection must not rest solely on storage an attacker can rewrite: after total local loss the broker learns the "current" generation from that same storage, making a served old capsule indistinguishable to the broker from legitimate recovery. The custodians are therefore the rollback trust root — contributor tooling caches the last generation it participated in or observed, every signed contribution states that generation, and the broker refuses to complete a quorum whose contributions attest a newer generation than the capsule being unlocked, so a rollback additionally requires deceiving $K$ custodians rather than only the broker. External generation anchors (repository mirror, offline exports, syslog trail) remain supporting evidence. Retiring a historical 1-of-1 metadata path with cryptographic assurance requires metadata-DEK rotation and authenticated object rewrite after that path is removed; suspected exposure of the repository master key requires repository rekeying or pack rewrite and cannot be repaired by resharing alone.

**Implementation steps:**

1. Define and implement the versioned envelope/recovery-capsule format at a deterministic pre-SlateDB location: authenticated header and policy binding the canonical logical capsule identifier, independently HKDF-derived AEAD wrappings of the metadata DEK and repository master key, the pinned broker identity public key, immutable local publication, automatic repository-backend mirroring, protected offline export, generation discovery, and custodian-anchored anti-rollback evidence.
2. Migrate Phase 18 key-in-DB repositories transactionally: while unlocked, copy and verify the repository master key in a new capsule generation, prove direct pack access from the capsule, publish all mirrors, then delete the SlateDB master-key record and retire standalone escrow records. Never remove the last verified recovery path, and retain no plaintext intermediate file.
3. Implement versioned Shamir sharing with a reviewed library and zeroizing buffers: split and reconstruct the capsule root secret, wrap each share with all repository/generation/policy/group/member/index/threshold bindings, and validate reconstruction by authenticated decryption of both capsule payloads before releasing either key.
4. Extend the envelope with `any_of`, `all_of`, and threshold expressions containing a single offline bootstrap member, hardware-backed member alternatives, or threshold groups. Support one or many `offline-argon2id`, `offline-keyfile`, `yubikey-piv`, `fido2-hmac-secret`, `azure-key-vault`, `aws-kms`, `aws-cloudhsm`, `gcp-kms`, and `gcp-cloud-hsm` members in any configured grouping, including a hardware seat where multiple enrolled YubiKeys unwrap the same member share as `1-of-N` alternatives; preserve Phase 18 envelopes only as an explicit migration input, not as a parallel quorum-compliant access path.
5. Implement `vaultic-key-broker` as a small separate service that performs locked startup and policy evaluation before any client initializes encrypted storage. It alone reads and validates the current capsule; non-interactive alternatives exist only in non-compliant or bootstrap configurations, so a quorum-compliant broker always enters the locked state and exposes only status and unlock-session operations until the policy is satisfied. Hold recovered keys and the broker identity private key in locked, non-dumpable, zeroizing memory; prohibit key persistence, warm-restart state, network listeners, and unrelated storage or repository operations; explicitly lock and revoke all leases on shutdown, fatal internal error, configured expiry, or policy activation requiring a new epoch.
6. Implement authenticated one-time unlock sessions in the broker API using a reviewed HPKE construction: session creation signed by the broker identity key pinned in the capsule policy, client-side signature verification and session-fingerprint display, the acknowledged `--unverified-session` disaster path with identity-key re-pinning in the first post-recovery generation, transcript and endpoint binding, custodian last-seen-generation attestation, Entra/AWS/GCP identity validation, contribution submission, duplicate-share and duplicate-principal rejection, replay/expiry rejection, progress reporting, permanent closure, unlock-epoch creation, and comprehensive secret zeroization.
7. Implement `vaultic index unlock status|contribute|lock` against the broker for interactive Entra, AWS IAM / IAM Identity Center, Google Cloud IAM, YubiKey PIV/FIDO2 PIN-touch, offline passphrase, and offline keyfile members, plus `vaultic index keys quorum create-group|add-member|remove-member|set-threshold|replace-member|verify`. Every mutation requires an unlocked broker, generates a fresh capsule root secret and shares, rewraps both payloads, publishes all immutable mirrors before activation, and either preserves the epoch only when the broker can prove the active keys and new policy remain consistent or atomically relocks it.
8. Implement effective-policy validation in `index keys status` and `index check`: enumerate every metadata and repository-master-key access route, compute the minimum number of independent custodians, label cloud groups principal-verified, YubiKey seats hardware-verified, and offline groups custody-assumed, and reject or flag legacy password keys, direct exports, standalone escrow, stale current-generation anchors, bootstrap bypasses, duplicated cloud keys, duplicated hardware seats, and overlapping principal/group authorization.
9. Implement a versioned local broker protocol with OS peer-credential checks, per-component authorization, challenge-response and replay resistance, protocol negotiation, lease IDs and expiry, disconnect revocation, and separate `metadata-dek` and `repository-master-key` capabilities. Bind `vaulticdb` metadata leases to its connection while allowing reacquisition after an authenticated restart; make pack-key and recovery leases short-lived and job-scoped. Authorize controlled client replacement through pinned release/deployment signing identities, component and version policy, executable ownership and path integrity, and anti-downgrade checks rather than path, account, caller claims, or immutable executable hashes alone.
10. Extend metadata-loss recovery so a broker with a successfully unlocked capsule can grant an explicitly authorized read-only recovery lease even when every SlateDB object is deleted or corrupt, without materializing the master key on disk. Add authenticated metadata rebuild handoff — the rebuilt database is encrypted under the recovered DEK, or under a fresh DEK published in a new capsule generation before any rebuild write — and require explicit acknowledgement and critical audit events before any recovery-mode operation.
11. Emit `auth`, `integrity`, and `lifecycle` events for capsule discovery and rollback rejection, unlock epoch and session lifecycle, accepted/rejected contributions, quorum completion, both-payload authentication, client authentication and authorization, lease grant/denial/expiry/revocation, explicit and automatic lock, break-glass use, policy changes, generation publication/mirroring, threshold downgrade, plaintext key export, and migration. Log identifiers, executable signing identity, component, and versions only, never keys, shares, or secret-bearing request data.
12. Document the complete model in `doc/070_encryption.rst`: capsule availability versus secrecy, master-key and metadata-key separation, effective-policy semantics, the session-verification ceremony (identity signature, fingerprint confirmation, custodian generation cache) and the fake-broker threat it addresses, unlock epochs and the absence of unattended broker restart in quorum-compliant mode, client restart/update workflows and authorization, lease scopes and expiry, explicit lock and optional maximum unlocked lifetime, post-unlock host-compromise limits, broker hardening and on-call expectations, YubiKey PIV and FIDO2 setup, PIN/touch requirements, backup-key enrollment, attestation and serial/public-key pinning, the `1-of-2 YubiKeys plus offline password` pattern, Azure per-key RBAC for Entra users/groups, AWS KMS key policy/IAM/Identity Center setup, AWS CloudHSM custody notes, Google Cloud KMS / Cloud HSM IAM setup, one-seat-per-group and non-overlap rules, offline custody and its custody-assumed limits, bootstrap → offline $2$-of-$3$ → cloud $2$-of-$4$ migration, removal of password/key-in-DB/standalone-escrow bypasses, online policy changes, old-generation and rollback limitations, break-glass recovery, total SlateDB destruction, rebuild, rotation, and periodic recovery exercises.

**Tests:** capsule-format round trips and independent purpose-binding tests for both payloads, including identical authentication of the same generation from local, mirror, and offline-export locations; malformed, partial, swapped, foreign-repository, stale-generation, and rollback capsules failing closed before SlateDB initialization; rollback tests where storage serves an old capsule and contributions attesting a newer last-seen generation abort the quorum; unsigned, wrongly signed, and identity-key-mismatched unlock sessions rejected by the contribute client, with the acknowledged `--unverified-session` path succeeding only with explicit per-custodian acknowledgement, emitting high-severity events, and re-pinning a fresh identity key; Shamir split/reconstruct across thresholds ($1$-of-$1$ through $3$-of-$5$), every $K$-subset succeeding and every $(K{-}1)$-subset failing; wrong, corrupt, duplicate, re-indexed, and mixed-policy/generation shares failing closed; unlock-session endpoint/transcript binding, Entra tenant/object-ID validation, AWS account/role/session-principal validation, Google Cloud project/service-account/group-principal validation, YubiKey PIV/FIDO2 credential/public-key pinning, wrong-token rejection, missing-touch rejection, wrong-PIN and locked-PIN handling, duplicate hardware-seat rejection, backup YubiKey success for the same hardware seat, duplicate-principal, replay, expiry, concurrent-session, and zeroization tests; offline Argon2id/keyfile, mocked YubiKey, and mocked Azure/AWS/GCP per-member isolation tests; effective-policy tests detecting every password, direct-key, escrow, bootstrap, stale-anchor, duplicate-key, duplicated hardware-seat, and overlapping-principal bypass and labeling cloud groups principal-verified, YubiKey seats hardware-verified, and offline groups custody-assumed; composed-policy tests proving `all_of(any_of(yubikey-primary, yubikey-backup), offline-password)` accepts either YubiKey plus the password but rejects password-only and both-YubiKeys-without-password; online membership/threshold/KEK changes proving fresh roots/shares and byte-identical SlateDB/data-pack ciphertext; key-in-DB migration crash tests at every publication/deletion boundary; full migration (single offline bootstrap → offline $2$-of-$3$ → cloud $2$-of-$4$ → bootstrap removal); physical deletion and authenticated corruption of all SlateDB objects followed by quorum unlock, in-memory master-key recovery, pack listing and restore in read-only mode without a plaintext key file; metadata rebuild handoff under both recovered-DEK and fresh-DEK paths; old-generation threat and metadata-DEK retirement tests; audit/syslog routing tests; documentation build and example-command validation.

Broker-specific tests cover locked startup; unlock-epoch creation and closure; explicit lock; configured maximum lifetime; broker crash, restart, and host-reboot simulation; lease revocation and zeroization; authenticated `vaulticdb` and `vaultic` restart without a new ceremony; approved signed upgrade and signing-key rotation; software-downgrade, unsigned-version, wrong-component, executable-replacement, mutable-path, caller-supplied identity, protocol-downgrade, and challenge-replay rejection; OS peer credentials and socket permissions; metadata-versus-pack capability separation; short-lived job and recovery leases; connection-bound metadata leases and reacquisition; disconnect and expiry handling; concurrent-job isolation; core-dump exclusion; and proof that inherited descriptors, environment variables, command lines, diagnostics, denials, and audit events contain no key or share material.

**Exit criterion:** the current immutable envelope/recovery capsule, available before and independently of SlateDB, is the sole managed authoritative holder of authenticated wrapped copies of both the metadata DEK and repository master key; the database contains no repository master key. A configured single-member, composed, or $K$-of-$N$ policy can combine any supported offline, YubiKey-backed hardware, and Azure/AWS/GCP cloud members, with cloud seats individually scoped to Entra, AWS IAM / IAM Identity Center, or Google Cloud IAM principals or groups and hardware seats pinned to enrolled YubiKey credentials, and status/`index check` report the minimum effective threshold across every access route, distinguishing principal-verified cloud seats, hardware-verified YubiKey seats, and custody-assumed offline seats. A quorum-compliant repository has no password-keyfile, direct-key, standalone-escrow, bootstrap, broker-credential, persisted warm-restart, duplicate-hardware-seat, duplicate-cloud-key, or principal-overlap bypass. Every broker or host restart blocks until a quorum completes, while authenticated `vaulticdb` and `vaultic` restarts and approved signed upgrades can obtain new least-privilege leases during the same live broker unlock epoch without another ceremony. Custodians contribute only to sessions signed by the broker identity pinned in the capsule, confirmed by an out-of-band fingerprint ceremony, and carrying their tooling's last-seen generation, so share harvesting and capsule rollback both require deceiving the configured quorum in addition to any storage or host compromise. One successful unlock keeps both keys in protected broker memory and releases them only through separately authorized metadata or short-lived job leases; complete loss or corruption of SlateDB still permits read-only pack listing and restore from a surviving capsule without writing a plaintext master key. The documented guarantee explicitly ends at compromise of the unlocked broker, host kernel/root boundary, or an authorized client holding its leased key. Policy and member changes publish fresh immutable capsule generations online without rewriting metadata or packs, while documented rotation rules accurately distinguish resharing from cryptographic revocation of copied old generations or exposed underlying keys. Operators can perform bootstrap, YubiKey, Azure/AWS/GCP cloud and offline quorum setup, broker deployment and hardening, client authorization and signed updates, lock/restart recovery, migration, verification, replacement, rollback response, total-metadata-loss recovery, rebuild, and periodic recovery exercises from the documentation alone.
